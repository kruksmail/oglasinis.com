<div class="paginationControl_Oglas">
Strana: <?php echo $this->current; ?> / <?php echo $this->pageCount; ?>
<br>
<?php
if ($this->pageCount) 
{
	// remove the system parameters
	unset ( $this->params ['module'] );
	unset ( $this->params ['controller'] );
	unset ( $this->params ['action'] );
	unset ( $this->params ['cenaod'] );
	unset ( $this->params ['cenado'] );
	unset ( $this->params ['valuta'] );
	unset ( $this->params ['submit'] );
}

if($this->pageCount > 1)
{
?>
<!-- Numbered page links -->
<?php
	for ($page = 1; $page <= $this->pageCount; $page++)
	{
		if ($page != $this->current) 
		{
			?>
<a title="Strana <?php echo $page;?>"
	href="<?php
			echo $this->url ( array_merge ( $this->params, array ('page' => $page ) ) );?>"><?php echo $page; ?></a>
<?php 
		}
		else 
		{
			echo $page;
		}
		echo " ";
		if($page < $this->pageCount)
		   echo "| ";
		
	}
}
?>
</div>